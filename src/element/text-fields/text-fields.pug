mixin text-fields-paste(type, placeholder_text)

    case type
        when "text-field"
            input.text-field(placeholder=placeholder_text)
        when "date"
            input.text-field.date-field(placeholder="ДД.ММ.ГГГГ")
        when "subscription"
            input.text-field.subscription(placeholder=placeholder_text)
